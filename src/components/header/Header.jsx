import StatusBar from './StatusBar';
import Title from './Title';

function Header() {
    return (
        <header>
        <Title>rflect</Title>
        <StatusBar />
    </header>
    )
}

export default Header;