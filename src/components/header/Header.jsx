import StatusBar from './StatusBar';
import Title from './Title';

function Header() {
    return (
        <header>
        <Title>rflect</Title>
        <StatusBar></StatusBar>
    </header>
    )
}

export default Header;